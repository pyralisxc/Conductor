# Initial GitHub Issue Set

## JC-1 — Establish repository verification and domain contracts
`npm run verify` passes; state model, provider contracts, and next-action tests exist.

## JC-2 — Register Jarvis Conductor GitHub App
Resolve installation scope, webhooks, least privilege, token mint/refresh.

## JC-3 — GitHub webhook ingestion
Initial issue, PR, check/workflow, and deployment events with signature verification and idempotency.

## JC-4 — Project orchestration config
Discover and validate `ORCHESTRATION.md`. CardForge is first real config.

## JC-5 — Preview branch manager
Create/verify Preview, create work branches from Preview, keep Preview reconciled with main.

## JC-6 — Work + PR adapter
Create/update work, bind PRs, project operational state.

## JC-7 — Preview integration policy
Authorized, checks passed, semantic conflict absent, hold policy, owner override, Preview health.

## JC-8 — Preview manifest
Generate main→Preview development narrative.

## JC-9 — Durable wait/runtime adapter
Vercel Workflow initially. Suspend on CI/deployment/review/timers.

## JC-10 — Owner gate queue
Founder semantics, Consequence, Experiential acceptance, Integration conflict, Main promotion.

## JC-11 — Owner Console read model
Mission Control, Project Cockpit, Work Detail, Preview Cockpit, Founder Control Center.

## JC-12 — Development Intelligence adapter
Project health, main/Preview awareness, overlap/blast radius, degraded mode.

## JC-13 — CardForge canary
1–2 concurrent implementation workers; no automatic main promotion.

## JC-14 — Conversational session registry
Session reference, work attachment, state, continuation preference, parent/return addresses.

## JC-15 — Work/Codex delegated operator
Bounded delegation contract and result return for browser/computer/environment-heavy work.

## JC-16 — Experimental Chat continuation bridge
Only after registry and safe fallback exist.
